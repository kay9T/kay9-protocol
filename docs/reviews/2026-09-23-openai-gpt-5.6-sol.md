# Launch-path model review — OpenAI GPT-5.6 Sol, 2026-09-23

| | |
|---|---|
| Model | GPT-5.6 Sol (OpenAI), reasoning effort high, as the model reported it |
| Interface | ChatGPT, one fresh thread with no prior project context |
| Date | 2026-09-23 |
| Commit | `db65339b9dadfa7c762e9a76190a1f5fa1894b92` of `kay9T/kay9-protocol`, tag `launch-review-1` |
| Scope | `KAY9Genesis`, `KAY9Token`, `KAY9TeamVesting`, `KAY9LiquidityLock`, their libraries, `Deploy.s.sol` and `Launch.s.sol` |
| Prompt | [`LAUNCH_PATH_REVIEW_PROMPT.md`](LAUNCH_PATH_REVIEW_PROMPT.md), verbatim |

This is a model review, not a professional audit. It is one of the two model families gate 6
asks for; the second is not from Anthropic, because a Claude model wrote part of this code.

## Dispositions

Each finding was checked against the source before it was decided.

| Finding | Held? | Disposition |
|---|---|---|
| M-01 — mainnet deployment can take a lookalike InitializerHook | Yes. `Deploy.s.sol` let `INITIALIZER_HOOK` override the address book on any chain, and `KAY9Genesis` checks the hook's interface and permission bits, not that the code is Uniswap's | **Fixed** in `Deploy.s.sol`: on mainnet the hook must equal the address book's, or the deployment reverts `NotCanonical`. The override remains for testnet, where the rehearsal deploys its own copy |
| M-02 — a tiny graduation threshold collapses the migration-outcome test | Yes. At `requiredCurrencyRaised = 1`, `raised / 2` is zero and a successful migration reads as failed | **Fixed in the contract and the script.** `KAY9Genesis.launch` refuses a threshold under 0.001 ETH (`MIN_REQUIRED_RAISE`), and the half-raise test rounds up. `Launch.s.sol` refuses the same threshold before a transaction is built. Regressions: `test_launchRejectsATinyGraduationRaise`, `test_rejectsAGraduationRaiseBelowTheMinimum` |
| L-01 — a successful migration can leave ETH dust in `KAY9Genesis` | Yes, and observed: the testnet rehearsal of 2026-09-23 left 48 wei in the vault after a good migration | **Fixed in the contract.** `settle()` now places any ETH the vault holds as a single-sided position above the clearing price, minted to the lock and locked (`LeftoverEthPlaced`). An amount too small to make one unit of liquidity stays where it is; there is no other path for it. Regressions: `test_settlePlacesLeftoverEth`, `test_aGoodMigrationLeavesAtMostDust` |

**Verification round** (same thread, at `launch-review-2` = `a327a830`): M-01 **closed**, M-02
**closed**, L-01 **narrowed**. The reviewer found that `recover()` did not run the new ETH cleanup,
and that a remainder too small to form one unit of liquidity can always stay. The first is fixed in
`launch-review-3`: `recover()` now ends with `_placeLeftoverEth`, like `settle()`. The second is
integer rounding in the position math, and the tests now pin it: after `settle()` or `recover()` the
vault holds **at most one wei** (`test_settlePlacesLeftoverEth`, `test_recoverAfterFailedMigration` in
`KAY9Recover.t.sol`). Property 4 is therefore stated as "every wei of the raise ends in locked
liquidity, except at most one wei of rounding", and that is how the prompt states it from
`launch-review-3` on. The reviewer also noted, without filing it, that the liquidity cap is applied
after a `uint128` conversion that could revert first at extreme prices; it found no reachable path
to that state from a real migration, and it is recorded here rather than changed.

The second model family reviews `launch-review-3`, and gate 8 needs a stack deployed from it.

## Final verification at `launch-review-4`

Same thread, after the second family's review had moved the commit. L-01 **narrowed** to
unplaceable rounding dust, with the `recover()` omission closed; every change checked, and one
residual raised, **R-01**: a gift of half the raise sent *before* `migrateAndSettle` still made a
good migration read as failed. Claude Opus 5.5 raised the same point independently (item 2.1 of
the second review's verification).

**R-01 fixed** at `launch-review-5` as the reviewer proposed: inside `migrateAndSettle` the outcome
is no longer read from the vault's balance. An official pool that did not exist before the call and
does after it was built by this launch's migration, so that is recorded as success before settling.
Regression `test_migrateAndSettleIgnoresAGiftSentBeforehand`: the vault is given the whole raise
before the call, the launch still migrates, settles and records success, the gift ends in locked
liquidity, and `recover()` is refused.

Running the mainnet fork suite on this commit exposed one more thing, in a test rather than in the
contract: the fork recovery test cleared every mocked call to stop a PositionManager failure, which
also cleared the chain-clock mock, so the auction's sweep reverted `AuctionIsNotOver`. Before
`UnsoldNotSwept` the vault swallowed that and recovered without sweeping, and the test passed. The
new check made it fail, which is the case F-4 of the second review was written about. The test now
restores the clock and asserts that the sweep happened; fork suite 3/3 at mainnet block 70,611,480.

**Property 4** is published as "every wei except at most one wei of rounding", which is what the
reviewer's "broken" refers to: the literal "every wei" wording it tested was withdrawn at
`launch-review-3`. **Property 6** stays "could not conclude" on the universal economic claim, as
the reviewer wrote it.

### The verification, verbatim

KAY9 Launch-Path Final Verification — launch-review-4
KAY9 Launch-Path Final Verification

Repository: kay9T/kay9-protocol
Reviewed commit: 5464d26e8810019d1f0e535327de692798637a6f (launch-review-4)
Previous OpenAI verification: a327a83054645a0cc332feaf19df42c619faa46b

The pinned tree was read at the SHA above. src/KAY9Genesis.sol begins:

// SPDX-License-Identifier: MIT
pragma solidity 0.8.26;
1. OpenAI L-01 disposition
L-01 — Successful migration can leave raised ETH in Genesis

NARROWED

Files: src/KAY9Genesis.sol:668-678, 701-750, 1133-1154

The part that remained open after my previous verification is now substantially fixed in both paths:

normal settlement calls _placeLeftoverEth(key) at 676-677;

recovery now also calls _placeLeftoverEth(key) at 748-750.

That closes my specific previous objection that recover() omitted the native-ETH cleanup.

It is still narrowed rather than closed under the literal original property. _placeLeftoverEth() deliberately leaves the balance untouched when the range cannot be constructed or the amount rounds to zero liquidity:

if (tickLower >= tickUpper) return;
...
if (liquidity == 0) return;

The regression tests show <= 1 wei for the exercised settlement and recovery configurations. That is strong evidence that the practical remainder for the intended launch parameters is negligible, and I agree that the rejected ~6e4 wei calculation had its ratio backwards for those economics. It is not, however, a contract-level proof that address(this).balance == 0 for every pricing/state combination the contract accepts.

Because _settleRemainder() can already have made settled == true before _placeLeftoverEth() returns, such unplaceable native dust has no later retry path.

So the security impact is now negligible dust, but the strict statement “every wei raised ends in liquidity” is still not mathematically enforced.

2. Findings from the launch-review-4 changes
R-01 — migrateAndSettle() does not actually eliminate pre-migration balance poisoning

Severity: Low — incomplete closure of F-3, not a new regression

Files: src/KAY9Genesis.sol:460-462, 651-665, 939-962, 972-989, 701-750

The new atomic function correctly removes the opportunity to transfer ETH between migration and settlement. However, _migrationOutcome() still decides success from Genesis's absolute ETH balance, so the same donation can be made before migrateAndSettle() begins.

Concrete sequence:

The auction graduates with raise R.

Before migration, an attacker transfers at least ceil(R / 2) to Genesis. receive() accepts it.

An honest caller executes migrateAndSettle().

lbpStrategy.migrate() successfully creates the official pool.

The function immediately calls _migrationOutcome().

_raiseCameBack() sees the attacker's pre-existing gift:

address(this).balance >= raised - raised / 2

and therefore classifies the successful migration as failed.

migrateAndSettle() returns at line 655 without locking the migration IDs and without _settle().

recover() can subsequently consume the gift and remaining KAY9 into the hookless recovery pool, reproducing the two-pool outcome F-3 described.

The attacker still sacrifices at least half the raise and I found no mechanism that returns that gift to them. This therefore remains a costly griefing/state-confusion path rather than theft. But the F-3 disposition “closed” is too strong: the post-migration transaction window is closed; the underlying absolute-balance poisoning is not.

There is also a related sequencing limitation: anyone can still call canonical LBPStrategy.migrate() directly. If that happens first, a later migrateAndSettle() calls migrate() again and reverts because the initializer has already been consumed. settle() remains available, so this does not strand assets, but the wrapper cannot guarantee that all migrations use its atomic path.

Suggested fix: for the migration performed inside migrateAndSettle(), do not use the pre-existing Genesis balance as the success signal. Because there can be no competing transaction during this call and LBPStrategy.migrate() itself is non-reentrant, initialization of the official pool during this call is unambiguous evidence of success. Record that success before entering _settle(). A before/after Genesis-balance delta would also be materially stronger than the absolute-balance test because ETH already present before the call would be excluded.

No other defect introduced by these changes was found

I specifically checked the requested areas:

migrateAndSettle() reentrancy

No finding.

KAY9Genesis.migrateAndSettle() is nonReentrant (651) and canonical LBPStrategy.migrate() is independently nonReentrant (lib/liquidity-launcher/src/strategies/lbp/LBPStrategy.sol:211-265).

The strategy can return ETH to Genesis during migration, but Genesis's receive() contains no state-changing callback logic. KAY9 has no token callback, the official InitializerHook has only beforeInitialize, and the migration's position additions do not invoke an attacker-controlled liquidity hook.

A revert in locking or settlement therefore also reverts the strategy migration performed earlier in the same transaction.

Migration position-ID range

No finding.

firstId is read immediately before LBPStrategy.migrate() and endId immediately afterward (652-657). No unrelated transaction can interleave.

KAY9's fixed position plan has one explicit full-range definition and at most the planner's full-range remainder position. Every ID in the range is additionally filtered by:

ownerOf(id) == address(liquidityLock)

before being locked. I found no path for the canonical migration to burn one of those freshly minted IDs before the loop and make ownerOf() unexpectedly revert.

Failed-migration branch

Sound.

If tryMigrate() fails, its state changes—including any PositionManager mint performed inside that reverting external self-call—roll back. LBPStrategy.migrate() then returns the reserve/raise through its recovery branch.

migrateAndSettle() sees succeeded == false and returns at 655; it does not attempt to lock a phantom ID range or settle. recover() remains the intended next step.

A non-graduated auction behaves similarly but remains on the ordinary failure/relaunch path.

Interaction with settle() and recover()

Sound, apart from R-01 and the L-01 dust qualification.

A successful atomic migration runs the same _settle() body used by public settle(). If _requireSwept(), position locking or either liquidity placement reverts, the whole migrateAndSettle() transaction—including migration—rolls back.

After a failed migration, recover() remains independent. It now performs the unsold-token assertion and both KAY9 and ETH remainder settlement.

A migration performed directly against LBPStrategy can still be finished with ordinary settle().

New timing bounds

No finding.

File: src/KAY9Genesis.sol:759-782

The new constraints correctly bound:

auction start to now + 25,920,000;

auction duration to 36,000..864,000;

claim to end..end+864,000;

migration to (end)..end+864,000.

The ordering of validation also prevents the uint64 additions from becoming a practical overflow path: startBlock is first constrained close to the current chain height and endBlock is constrained relative to it.

The step parser is bounded by the already-checked length % 8 == 0, and the CCA constructor continues to enforce the complete schedule's 100% total and exact window span. MAX_STEP_MPS therefore adds a restriction rather than replacing the underlying CCA validation.

KAY9LiquidityLock constructor validation

No finding.

File: src/KAY9LiquidityLock.sol:78-106

The constructor now verifies before deployment succeeds that:

the FeeSplitter uses the same PositionManager; and

its immutable split list contains the configured BeneficiaryVault.

The ABI layout matches the committed upstream FeeSplit definition. The checks are against fixed mainnet dependencies and occur before the lock becomes usable, so I found no exploitable constructor reentrancy or state inconsistency introduced by them.

Dependency pinning

No finding.

The dependency sources used by the compiler are now present in the reviewed tree, including BlockNumberish, v4 core/periphery, OpenZeppelin, Permit2, Solady and the other imported files. setup.sh skips already-populated committed dependency directories. The locally modified Permit2 pragmas are also already committed in their modified form.

The reviewed SHA therefore determines materially more of the build than the earlier tags did.

3. Final verdict on the nine properties
#	Verdict	Final assessment
1	HOLDS	KAY9Token.sol:17-29 mints exactly 1,000,000,000e18 once. No owner/minter/proxy/pause/tax/blacklist path exists; subsequent supply reduction comes from ERC20Burnable and allowance-authorized burnFrom.
2	HOLDS	Team allocation remains exactly 90M; Genesis launch allocation is exactly 910M, split by the fixed LBP parameters into 455M auction + 455M reserve (KAY9Genesis.sol:261-268, 805-829).
3	HOLDS	Currency, recipients, pool fee, tick spacing, hook and position recipient remain contract-constructed rather than owner-selectable. Mainnet InitializerHook and launch-script ETH/USD feed are now canonical-address constrained, and ownership cannot be renounced (685-688). There is still no withdrawal path.
4	BROKEN	In substance almost all raised ETH is forced into locked liquidity, and unsold KAY9 must now be successfully swept before settlement. Both normal settlement and recovery run _placeLeftoverEth. But the helper explicitly permits an unplaceable native remainder to stay in Genesis, with current tests bounding it to at most one wei. The literal “every ETH” claim therefore remains false.
5	HOLDS	A non-graduated CCA retains bidder currency for full refunds. Relaunch still requires observable Failed, explicit markFailed(), and the 48-hour timestamp. The new time bounds prevent owner-selected multi-month/year lockups.
6	COULD NOT CONCLUDE	Recovery itself remains pinned to the auction clearing price and rejects an already initialized recovery key at another price. I found no direct profit mechanism from R-01: the balance-poisoning actor sacrifices at least half the raise. But “nobody can profit from forcing that path” is a universal economic/MEV assertion that the source review does not prove, and R-01 confirms that a healthy migration can still be forced into the recovery classification at sufficient cost.
7	HOLDS	KAY9TeamVesting.sol:145-161 releases nothing until launch.settled(). The immutable amounts remain 10M/40M/40M on the fixed deployment calendar. A delayed launch can make an already-passed tranche immediately releasable at settlement, but never before settlement.
8	HOLDS	The lock still has no owner-controlled exit. The preferred atomic path now transfers migration positions onward to the verified FeeSplitter immediately. Positions created by a direct strategy migration may temporarily remain owned by KAY9LiquidityLock, but that contract itself exposes no route to withdraw them anywhere except its immutable FeeSplitter.
9	HOLDS	The dependency is now part of the pinned tree. lib/blocknumberish/src/BlockNumberish.sol:23-55 detects ArbSys and _getBlockNumberish() uses arbBlockNumber() whenever that precompile is present. Genesis launch validation/state resolution and the CCA/LBP timing gates use that helper, not block.number.
Final register
Item	Status at 5464d26e
OpenAI M-01 — mainnet InitializerHook substitution	Closed
OpenAI M-02 — tiny-raise migration misclassification	Closed
OpenAI L-01 — native raise residual	Narrowed to unplaceable rounding dust; recovery omission closed
F-3 atomic migration/settlement	Improved but not fully closed — pre-migration balance poisoning remains
migrateAndSettle reentrancy	No defect found
migrateAndSettle minted-ID range	No defect found
Failed-migration behavior	No defect found
New timing bounds	No defect found
Emission-step cap	No defect found
Unsold-sweep assertion	No defect found
FeeSplitter constructor wiring check	No defect found
Ownership-renounce prohibition	No defect found
Mainnet ETH/USD-feed restriction	No defect found
Dependency/build pinning	No defect found
Model

GPT-5.6 Sol — high reasoning effort.


## The review, verbatim

// SPDX-License-Identifier: MIT
pragma solidity 0.8.26;

KAY9 Launch-Path Model Review

Reviewed commit: db65339b9dadfa7c762e9a76190a1f5fa1894b92

I read the requested launch-path contracts at that exact commit and, only where necessary to understand their behavior, the Uniswap Liquidity Launcher / CCA code they invoke.

Findings
M-01 — Mainnet deployment can substitute a non-canonical lookalike for the InitializerHook

Severity: Medium

Files: script/Deploy.s.sol:241-245; src/KAY9Genesis.sol:404-420

Deploy.s.sol defaults to the address-book InitializerHook but permits an unrestricted environment override:

solidity
cfg.initializerHook = vm.envOr("INITIALIZER_HOOK", book.initializerHook);

The Genesis constructor then tries to establish that this is the canonical InitializerHook by checking code presence, ERC-165 support, authorized(), the hook permission bits, and v4 address validity.

Those checks establish the interface and address shape. They do not establish that the contract is Uniswap's InitializerHook or that its beforeInitialize implementation actually enforces the value returned by authorized().

A crafted contract can therefore:

be CREATE2-mined to an address carrying exactly BEFORE_INITIALIZE_FLAG;

report support for IInitializerHook;

return the expected LBPStrategy from authorized();

nevertheless allow arbitrary callers through beforeInitialize.

The deployment passes KAY9Genesis._validatePoolHook().

After KAY9 launches but before its migration, an attacker can then initialize the advertised official pool key. KAY9's LBP migration subsequently fails because the pool already exists and the launch has to use the recovery pool instead.

I did not find a path from this alone to steal the raise—the recovery design protects it—but the deployed launch would no longer have the claimed canonical/unsquattable InitializerHook. The mistake is immutable.

Suggested fix

On Robinhood mainnet, do not permit INITIALIZER_HOOK to override the address book:

solidity
cfg.initializerHook = book.initializerHook;

Keep the override only for testnet/rehearsal chains. For additional defense, verify the expected deployed runtime/code hash or another canonical identity commitment rather than treating ERC-165 plus self-reported authorized() as proof of implementation identity.

M-02 — Tiny valid raises collapse the migration-success discriminator and can send a successful launch through recover()

Severity: Medium

File: src/KAY9Genesis.sol:674-689, 846-899

The launch accepts every nonzero graduation threshold:

solidity
if (p.requiredCurrencyRaised == 0) revert InvalidRequiredRaise();

Migration success is later distinguished from migration failure by testing whether Genesis holds at least half of the auction's gross raise:

solidity
return raised != 0 && address(this).balance >= raised / 2;

Integer division makes this predicate degenerate for raised == 1:

raised / 2 == 0
balance >= 0 == true

So a successfully migrated auction whose credited raise is one wei is necessarily read as though its raise came back.

A concrete sequence is:

The owner supplies otherwise-valid pricing/timing with requiredCurrencyRaised = 1.

The auction credits one wei and graduates.

LBPStrategy.migrate() succeeds and initializes the official hooked pool.

The official pool therefore exists.

_raiseCameBack() nevertheless returns true because 1 / 2 == 0.

_migrationOutcome() reports failure instead of success.

settle() refuses to execute.

If Genesis has zero ETH, recover() also initially refuses because ethAmount == 0.

Any account can then send one wei to Genesis and invoke recover().

The launch creates a second, hookless recovery pool even though the official migration actually succeeded, and poolKey() subsequently resolves to that recovery pool.

Without the one-wei donation, settlement/team unlocking can remain blocked; with it, the launch permanently ends with two liquidity pools and a false recorded migration outcome.

The production Launch.s.sol is unlikely to generate such an economically tiny launch, but the on-chain owner interface explicitly accepts it. The advertised invariants therefore do not hold over the contract's accepted parameter space.

The same class of ambiguity exists for other very small raises if successful migration dust approaches the truncated half-raise threshold.

Suggested fix

At minimum, use ceiling division:

solidity
uint256 halfRaised = raised / 2 + raised % 2;

and reject economically meaningless graduation thresholds.

More robustly, establish and enforce a minimum raise comfortably above the proven maximum successful-migration currency dust, then compare returned ETH against that bound. The current half-raise heuristic explicitly depends on “success dust ≪ raise” but the contract never enforces the premise.

L-01 — Successful migration may permanently leave raised-ETH dust in Genesis

Severity: Low

File: src/KAY9Genesis.sol:423-425, 599-607, 872-899

The code itself acknowledges at 874-875 that a successful LBP migration can return currency dust to Genesis:

“A successful migration leaves this contract with currency dust…”

That follows the canonical LBP behavior: after position creation, unused currency is forwarded to migrationParams.recipient, which KAY9 fixes to Genesis.

Once migration is classified as successful, settle():

records success;

sweeps unsold KAY9;

calls _settleRemainder(), which handles KAY9 only.

It does not consume address(this).balance.

recover() is the only Genesis function that subsequently spends native ETH, but it rejects a successfully migrated launch.

Consequently, any successful-migration currency dust is permanently trapped in Genesis rather than ending in the locked liquidity position.

This does not provide an extraction path and the expected amount is dust, but it contradicts property 4 as written: not literally every wei raised is guaranteed to end in locked liquidity.

Suggested fix

Either:

consume any success-path ETH residual during settle() together with the remaining KAY9 into locked liquidity; or

formally derive and enforce a maximum residual, expose it explicitly as accepted unrecoverable dust, and change the claimed invariant from “every ETH raised” accordingly.

A withdrawal is not an appropriate fix because that would weaken the stronger no-withdraw property.

Claimed properties
#	Result	Assessment
1	holds	KAY9Token mints 1,000,000,000e18 once in its constructor and exposes no further mint/admin/pause/tax/blacklist path. Supply can decrease through standard ERC20Burnable; burnFrom additionally permits allowance-authorized burning, but there is no privileged burn of arbitrary holders.
2	holds	Genesis transfers exactly 90M to vesting and retains 910M. _buildParams() fixes reservedTokenAmountForLP = 455M; canonical LBPStrategy therefore derives the auction amount as 910M - 455M = 455M.
3	broken	Once Genesis is deployed, its owner cannot redirect funds or alter the fixed pair, fee, spacing, recipients or hook. But the end-to-end deployment path does not guarantee the hook is actually Uniswap's canonical InitializerHook: Deploy.s.sol permits an arbitrary INITIALIZER_HOOK, and Genesis authenticates only its observable interface/address characteristics. See M-01.
4	broken	The 100%-LP allocation schedule and zero-fee CCA configuration route the substantive raise into liquidity, and unsold KAY9 goes only to Genesis for single-sided liquidity/burning. However successful migration can return native rounding dust to Genesis, and successful settle() never consumes it. See L-01.
5	holds	For a non-graduated canonical CCA, exitBid() processes the bid with zero spend and therefore refunds the complete bid. Currency cannot be swept as a successful raise. Genesis requires launchState == Failed, explicit markFailed(), and block.timestamp >= marked + 48 hours before entering the relaunch path. The reserve and auction supply are recovered before the 910M balance check.
6	could not conclude	The mechanical part holds: recover() derives the v4 price from the auction's clearing price and refuses a pre-existing hookless pool whose current initialization price differs. I found no direct extraction from the documented “donate ≥ half the raise to force recovery” route. I cannot prove the stronger universal claim that nobody can profit from every possible forced-recovery/MEV/multi-pool strategy without adversarial economic simulation.
7	holds	KAY9TeamVesting.releasable() returns zero until launch.settled(). The allocation is exactly 90M in 10M/40M/40M cumulative tranches. Deploy.s.sol requires the latter timestamps to equal CalendarMonths.addMonths(tge, 6) and addMonths(tge, 12).
8	holds	Genesis-created residual/recovery positions are minted to KAY9LiquidityLock and immediately passed through lock(). The normal migration position is also minted to the lock; forwarding that initial NFT to FeeSplitter requires the documented permissionless lock(tokenId) call, but while it remains in KAY9LiquidityLock there is still no alternative transfer/withdraw path. The canonical FeeSplitter exposes fee collection and liquidity increase, not withdrawal of LP principal.
9	holds	Genesis uses _getBlockNumberish() for start validation and launch-state resolution; Launch.s.sol derives start/end from genesis.chainBlockNumber(). Its block.number use is display-only. The CCA StepStorage and LBP migration gating also use _getBlockNumberish(). Uniswap documents the blocknumberish-aware CCA factory used here as the post-fix deployment and documents BlockNumberish as routing Arbitrum/Orbit chains through ArbSys.arbBlockNumber(). 
GitHub
+1
Checked without a finding

I checked the fixed-supply mint and burn surface; the 90M/910M constructor split; the LBP-derived 455M/455M auction/reserve split; Permit2 approval/revocation; the fixed native-ETH/KAY9 pool key; 1% static fee and spacing 200; position and remainder recipients; failure/relaunch state transitions; auction refund behavior; clearing-price conversion against Uniswap's TokenPricing; calendar-month computation; single-sided leftover placement; recovery-pool price pinning; beneficiary registration; and the FeeSplitter's lack of an LP-principal withdrawal operation.

I also checked the canonical CCA protocol-fee behavior. The factory architecture can support protocol fees generally, but the factory deployment KAY9 targets is documented with a zero protocol-fee controller; consequently I did not report the canonical CCA fee mechanism as a violation of KAY9's raise-to-liquidity property. Uniswap's CCA documentation confirms that a zero controller means no protocol-fee deduction. 
GitHub

Not covered / could not verify

I did not review KAY9AccessVault, KAY9AuditHub, KAY9Registry, KAY9AuditorRegistry, KAY9ScanRegistry, the website, or off-chain services.

I did not re-audit Uniswap v4, Permit2, Liquidity Launcher, Continuous Clearing Auction, FeeSplitter or BeneficiaryVault as independent third-party products. I inspected the paths KAY9 relies on to determine whether KAY9 uses them correctly.

I did not run a Foundry fuzz/invariant suite, Slither, symbolic execution, or a Robinhood mainnet-fork execution in this review. The findings above are source-level review findings.

I did not independently prove the economic assertion in property 6 against all flash-liquidity, sandwich, cross-pool and MEV constructions; that is why that property is explicitly could not conclude rather than holds.

I did not independently attest live Robinhood bytecode equality for every configured Uniswap dependency. Upstream Uniswap documentation was used where exact deployed dependency behavior—not KAY9-owned source—was necessary, particularly for BlockNumberish.

Model

GPT-5.6 Sol — reasoning effort: high.